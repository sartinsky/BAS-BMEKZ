
&НаКлиенте
Процедура СтатьиЗатратСтатьяЗатратПриИзменении(Элемент)
	
	//Объект.СтатьиЗатрат.Получить(Элементы.СтатьиЗатрат.ТекущаяСтрока).ВидЗатрат = Объект.СтатьиЗатрат.Получить(Элементы.СтатьиЗатрат.ТекущаяСтрока).СтатьяЗатрат.ВидЗатрат;
	Элементы.СтатьиЗатрат.ТекущиеДанные.ВидЗатрат = ПолучитьВидЗатрат(Элементы.СтатьиЗатрат.ТекущиеДанные.СтатьяЗатрат);
	
КонецПроцедуры

&НаСервере
Функция ПолучитьВидЗатрат(СтатьяЗатрат)
	
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(СтатьяЗатрат, "ВидЗатрат").ВидЗатрат;
	
КонецФункции