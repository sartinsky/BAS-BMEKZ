#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если Объект.DRU_КодВидаХранения = 0 Тогда
		ТекстСообщения = НСтр("ru='Присвойте код вида хранения!';uk='Призначте код виду зберігання!'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения, , "Объект.DRU_КодВидаХранения", , Отказ);	
	КонецЕсли;

КонецПроцедуры 

#КонецОбласти
