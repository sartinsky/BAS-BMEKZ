

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ЭтоГруппа Тогда
		
		Если СтрДлина(СокрЛП(Код)) <> 2 Тогда
			
			ШаблонОшибки = НСтр("ru='Код региона должен состоять из двух символов!';uk='Код регіону повинен складатися із двох символів!'");
			ТекстОшибки  = ОбщегоНазначенияБПКлиентСервер.ПолучитьТекстСообщения(
				"Поле", "Корректность", НСтр("ru='Код региона';uk='Код регіону'"),,, ШаблонОшибки);
			
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки,, "Объект.Код",, Отказ);
			
		КонецЕсли;
		
		
	КонецЕсли;
КонецПроцедуры

