#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий
	
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда	
		Возврат;	
	КонецЕсли;
	
	Если ДополнительныеСвойства.Свойство("ПоследнийНомер") Тогда
		НомерЖурнала = РегистрыСведений.ИНАГРО_НумерацияЖурналовЭлеватора.СоздатьМенеджерЗаписи();
		НомерЖурнала.ВидЖурнала          = ВидЖурнала;
		НомерЖурнала.ТипЖурнала          = Ссылка;
		НомерЖурнала.ТекущийНомерЖурнала = ЭтотОбъект.ДополнительныеСвойства.ПоследнийНомер;			
		НомерЖурнала.Записать();
	КонецЕсли;

КонецПроцедуры

#КонецОбласти 

#КонецЕсли