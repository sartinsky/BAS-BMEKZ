
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ВладелецФормы = ПараметрыВыполненияКоманды.Источник;
	
	Если НЕ ВладелецФормы.Окно = ПараметрыВыполненияКоманды.Окно Тогда
		ТекстПредупреждения = НСтр("ru='Данные по учету затрат не редактируются в отдельном окне';uk='Дані за обліком витрат не редагуються в окремому вікні'");
		ПоказатьПредупреждение(,ТекстПредупреждения);
	КонецЕсли; 
	
	ПараметрыОткрытия = Новый Структура("ДатаПриема");
	ЗаполнитьЗначенияСвойств(ПараметрыОткрытия, ВладелецФормы);
	
	ПараметрыОткрытия.Вставить("СотрудникСсылка", ПараметрКоманды);

	ОткрытьФорму("Справочник.Сотрудники.Форма.УчетЗатрат", 
		ПараметрыОткрытия, 
		ВладелецФормы, 
		, 
		ВладелецФормы.Окно);
		
КонецПроцедуры
