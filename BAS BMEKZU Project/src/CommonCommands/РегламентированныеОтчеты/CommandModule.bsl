
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	
	ФормаУправлениеОтчетностью = ОткрытьФорму("Обработка.ОбщиеОбъектыРегламентированнойОтчетности.Форма.УправлениеОтчетностью", , 
	ПараметрыВыполненияКоманды.Источник, 
	ПараметрыВыполненияКоманды.Уникальность, 
	ПараметрыВыполненияКоманды.Окно);
	
	ПараметрыСообщения = Новый Структура;
	ПараметрыСообщения.Вставить("ПоказыватьКнопки", Истина);
	
	Если ФормаУправлениеОтчетностью.Организация.Количество() > 0 Тогда
		
		ПараметрыСообщения.Вставить("Организация", ФормаУправлениеОтчетностью.Организация[0].Значение);
		
	КонецЕсли;
	
	
КонецПроцедуры

